# Preserve the frozen P2 corpus byte-for-byte on every platform.
# Prevent Git for Windows from converting LF to CRLF and breaking the canonical SHA-256 gate.
data/adaptasi_indonesia/corpus_whatsapp_10000.csv -text
